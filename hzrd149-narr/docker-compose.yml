version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: hzrd149-narr_web_1
      APP_PORT: 7049
      PROXY_AUTH_ADD: "false"

  web:
    image: ghcr.io/hzrd149/narr:latest
    restart: on-failure
